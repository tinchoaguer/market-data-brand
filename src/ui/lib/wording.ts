/**
 * Shared chrome strings for UI kit primitives.
 * Sourced from the package locale catalog — do not hardcode English in components.
 */
import en from '../../locales/en.json'

export const uiWording = en

export type UiWording = typeof uiWording
